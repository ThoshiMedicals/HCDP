
    showModal('Scan Document / OCR',`
      <div class="ocr-box">
        <div class="ocr-drop">
          <div>${icon('sparkles')}</div>
          <strong>Upload or paste invoice text</strong>
          <p>For this HTML preview, OCR is simulated locally. Paste invoice line text or choose a file name, then extract rows into inventory fields.</p>
          <input class="input" type="file" id="ocrFile" accept="image/*,.pdf,.txt" />
        </div>
        <div class="field full"><label>OCR text / invoice lines</label><textarea class="input" id="ocrText" placeholder="Example: Gloves Small qty 10 batch GLO-102 expiry 2026-09-30"></textarea></div>
        <div class="grid cols-3">
          ${metric('OCR review','Required','file','warning')}
          ${metric('Duplicate invoice','Checked','alert','info')}
          ${metric('Inventory update','After review','box','success')}
        </div>
      </div>`,
      `<button class="btn line" onclick="closeModal()">Cancel</button><button class="btn teal" onclick="extractInventoryOCR()">Auto extract to rows</button>`);
  