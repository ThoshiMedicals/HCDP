const p=id?(state.records.printers||[]).find(x=>x.id===id):{};showModal(id?'Edit Printer':'Add Printer',`<form id="printerFleetForm" class="form-grid"><div class="field"><label>Printer name *</label><input class="input" name="name" value="${escapeHtml(p?.name||'')}" placeholder="e.g. CH Reception OKI"></div><div class="field"><label>Location *</label><select class="select" name="location"><option value="">Select location</option>${state.locations.map(l=>`<option value="${l.id}" ${p?.location===l.id?'selected':''}>${escapeHtml(l.name)}</option>`).join('')}</select></div><div class="field"><label>Room / area</label><input class="input" name="room" value="${escapeHtml(p?.room||'')}"></div><div class="field"><label>Brand</label><select class="select" name="brand"><option>OKI</option><option>Brother</option><option>Kyocera</option><option>Canon</option><option>HP</option><option>Other</option></select></div><div class="field"><label>Model</label><input class="input" name="model" value="${escapeHtml(p?.model||'')}"></div><div class="field"><label>Serial / asset ID</label><input class="input" name="serial" value="${escapeHtml(p?.serial||'')}"></div><div class="field"><label>Hostname</label><input class="input" name="hostname" value="${escapeHtml(p?.hostname||'')}"></div><div class="field"><label>IP address</label><input class="input" name="ipAddress" value="${escapeHtml(p?.ipAddress||'')}"></div><div class="field"><label>Network segment / VLAN</label><input class="input" name="networkSegment" value="${escapeHtml(p?.networkSegment||'')}"></div><div class="field"><label>Connection path</label><select class="select" name="connectionPath"><option>Direct LAN</option><option>WiFi</option><option>Print Server</option><option>USB to workstation</option><option>VPN / remote print</option></select></div><div class="field"><label>Print queue name</label><input class="input" name="printQueue" value="${escapeHtml(p?.printQueue||'')}"></div><div class="field"><label>Driver / package</label><input class="input" name="driverName" value="${escapeHtml(p?.driverName||'')}"></div><div class="field"><label>Black toner %</label><input class="input" type="number" name="tonerBlack" value="${escapeHtml(p?.tonerBlack||'100')}"></div><div class="field"><label>Colour toner %</label><input class="input" type="number" name="tonerColour" value="${escapeHtml(p?.tonerColour||'100')}"></div><div class="field"><label>Mono counter</label><input class="input" type="number" name="monoCounter" value="${escapeHtml(p?.monoCounter||'0')}"></div><div class="field"><label>Colour counter</label><input class="input" type="number" name="colourCounter" value="${escapeHtml(p?.colourCounter||'0')}"></div><div class="field"><label>Service due</label><input class="input" type="date" name="serviceDue" value="${escapeHtml(p?.serviceDue||'')}"></div><div class="field"><label>Status</label><select class="select" name="status"><option>Registered</option><option>Online</option><option>Low toner</option><option>Offline</option><option>Fault open</option><option>Service due</option><option>Resolved</option></select></div><div class="field"><label>Fallback printer</label><input class="input" name="fallbackPrinter" value="${escapeHtml(p?.fallbackPrinter||'')}"></div><div class="field"><label>Supplier / support</label><input class="input" name="supplier" value="${escapeHtml(p?.supplier||'')}"></div><div class="field full"><label>Notes</label><textarea class="textarea" name="notes">${escapeHtml(p?.notes||'')}</textarea></div></form>`,`<button class="btn line" onclick="closeModal()">Cancel</button><button class="btn teal" onclick="savePrinterFleet('${id||''}')">Save Printer</button>`)