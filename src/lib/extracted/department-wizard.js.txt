
    const rec=id?(state.records.departments||[]).find(x=>x.id===id):{};
    showModal(id?'Edit Department':'Create Department',`<form id="departmentForm" class="form-grid"><div class="field"><label>Department name <span class="required">*</span></label><input class="input" name="name" value="${escapeHtml(rec?.name||'')}" placeholder="e.g. Facilities"></div><div class="field"><label>Manager / owner <span class="required">*</span></label><input class="input" name="manager" value="${escapeHtml(rec?.manager||'')}" placeholder="Responsible person"></div><div class="field"><label>Users / teams</label><input class="input" name="users" value="${escapeHtml(rec?.users||'')}" placeholder="Reception, Nursing, IT"></div><div class="field"><label>Default dashboard</label><select class="select" name="defaultDashboard"><option>Overview</option><option>Tasks</option><option>Tickets</option><option>Compliance</option><option>Finance</option></select></div><div class="field full"><label>Purpose</label><textarea class="textarea" name="purpose" placeholder="Describe what this department manages...">${escapeHtml(rec?.purpose||'')}</textarea></div><div class="field"><label>Status</label><select class="select" name="status"><option ${rec?.status==='Active'?'selected':''}>Active</option><option ${rec?.status==='Draft'?'selected':''}>Draft</option><option ${rec?.status==='Archived'?'selected':''}>Archived</option></select></div><div class="field"><label style="display:flex;gap:8px;align-items:center;margin-top:28px"><input type="checkbox" name="workflowBuilder" ${rec?.workflowBuilder?'checked':''}> Enable workflow builder</label><div class="field-hint">Keep disabled unless future phase is approved.</div></div></form>`,`<button class="btn line" onclick="closeModal()">Cancel</button><button class="btn teal" onclick="saveDepartmentModal('${id}')">Save Department</button>`);
  