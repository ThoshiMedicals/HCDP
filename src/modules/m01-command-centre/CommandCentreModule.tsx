"use client";

/**
 * Module 1 entry — temporary adapter over the existing workspace.
 * Full migration into src/modules/m01-command-centre is deferred to preserve behaviour.
 */
export { DashboardWorkspace as CommandCentreModule } from "@/components/workspaces/DashboardWorkspace";
