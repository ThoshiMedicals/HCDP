"use client";

/**
 * Module 3 entry — temporary adapter over the existing workspace.
 * Full migration into src/modules/m03-organisation-access is deferred to preserve behaviour.
 */
export { OrganisationWorkspace as OrganisationAccessModule } from "@/components/workspaces/OrganisationWorkspace";
