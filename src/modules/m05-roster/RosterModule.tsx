"use client";

import { RosterWorkspace } from "./RosterWorkspace";

/**
 * Module 5 entry — renders the full Roster & Shift workspace.
 */
export function RosterModule() {
  return <RosterWorkspace />;
}
