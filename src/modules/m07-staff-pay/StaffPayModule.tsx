"use client";

import { StaffPayWorkspace } from "./StaffPayWorkspace";

/**
 * Module 7 entry — Batch 1 foundation shell.
 * Does not use HTML iframe / legacy fallback as the main experience.
 */
export function StaffPayModule() {
  return <StaffPayWorkspace />;
}
