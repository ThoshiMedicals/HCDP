"use client";

import { TrainingWorkspace } from "./TrainingWorkspace";

/**
 * Module 11 entry — renders the full Training workspace.
 */
export function TrainingModule() {
  return <TrainingWorkspace />;
}
