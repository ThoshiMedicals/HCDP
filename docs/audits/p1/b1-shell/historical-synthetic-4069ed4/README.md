# Historical / synthetic P1-B1 evidence (tip 4069ed4)

These screenshots were produced using **removed** runtime acceptance hooks:

- `ShellHarnessProbe` + `sessionStorage p1-b1-harness-probe` (DetailPanel probe shots)
- `sessionStorage p1-b1-harness-force-inbox-error` (forced Action Inbox error shot)

**Classification:** historical / synthetic — **not** ordinary product-runtime evidence.

They are preserved for audit trail only. Do not treat them as naturally occurring product states.

Replacement coverage:

- Component/contract unit tests: `src/components/shell/tests/p1-b1-shell-primitives.test.ts`
- Product drawer browser evidence remains in live `docs/audits/p1/b1-shell/shots/interaction-drawer-*`
