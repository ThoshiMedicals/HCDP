# Wave 6 — M07 Screen & Functional-Action Matrix

**Status:** PLANNING ONLY · Batches 1–4  
**Parity:** Approve Location CONSOLIDATED; Award Rules RELOCATED to M07 Settings

---

## 1. Pay Run Overview
Create period (one open ordinary/entity); refresh intake; Lock (approver/admin only after recon); no unlock; clinic readiness summary.

## 2. People Review
M04 identity read-only; external payroll employee id edit (permissioned, audited relink); rates redacted for clinic managers; doctor excluded; classification→rule map status.

## 3. Exceptions
Waive = approver/admin; non-waivable labelled; clinic-tagged exceptions.

## 4. Variances
Clinic filter; M05 read; no source rewrite.

## 5. Adjustments
PPA linked to locked source; code list pickers.

## 6. Approval
Submit (clerk); final approve evaluates **all included clinics’ readiness** (Q21); incomplete readiness blocks; excluded clinics audited; SoD enforced. No location-approved state.

## 7. Export
Select active export profile (operator cannot edit); generate CSV+JSON; retain profile id+version; min-PII default; sensitive marking when rates included.

## 8. Reconciliation
Record external results; reconciled-unlocked reminder; does not lock.

## 9. History
Immutable calc/export/rule/profile versions; filter/clear.

## 10. Reports
Operational; monetary only with rate.view; non-certified copy.

## 11. Settings
Cadence/SoD; **prep rule tables**; **code tables**; **export profiles** (admin); no award editing in M04; no bank fields; no unlock; no doctor override.

## Omitted
Process Final Pay; Xero-only domain; Reopen unlock; auto-lock; Approve Location state; Staff-hub award editing; TFN/bank forms; Export operator Lock; CSS redesign.
